This directory is legacy code from the pre-monorepo SQLite prototype.

Active product code lives under `apps/web/src` and the shared packages live under `packages/`.

The only actively referenced path here is the Drizzle SQLite schema used by [drizzle.config.ts](/Users/rajatyadav/LegalPOC/drizzle.config.ts). Do not add new product features under `src/`; add them under `apps/web/src` instead.
