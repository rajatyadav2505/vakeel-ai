This directory is archived material from the pre-monorepo SQLite prototype.

Active product code lives under `apps/web/src` and the shared packages live under `packages/`.

The only retained file here is [`src/lib/db/schema.ts`](/Users/rajatyadav/LegalPOC/src/lib/db/schema.ts), which is still referenced by [`drizzle.config.ts`](/Users/rajatyadav/LegalPOC/drizzle.config.ts) for the archived SQLite schema. Do not add new product features under `src/`.
